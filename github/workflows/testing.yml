name: Test React App
on: push
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Git repository code
              usec: actions/checkout@v3
            - name: Install dependecies
              run: npm ci
            - name: Test application
              run: npm run test
